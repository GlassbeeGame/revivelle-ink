---
import LocationPageLayout from '../../layouts/LocationPageLayout.astro';
import { getLocationBySlug } from '../../data/locations.js';

const location = getLocationBySlug('spanish-springs');
if (!location) return Astro.redirect('/locations');
---

<LocationPageLayout location={location} />
