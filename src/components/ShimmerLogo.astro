---
// ShimmerLogo - Gold highlight animation behind the logo
// Highlight reveals from left to right like it's being drawn, then STAYS
---

<div class="logo-hero">
  <!-- Logo container with layered effect -->
  <div class="logo-hero__container">
    <!-- Gold highlight - BEHIND the logo, reveals left to right -->
    <img 
      src="/highlight.svg" 
      alt="" 
      class="logo-hero__highlight"
      aria-hidden="true"
    />
    
    <!-- Main logo on TOP -->
    <img 
      src="/Logo.svg" 
      alt="Revivelle Ink" 
      class="logo-hero__logo"
    />
  </div>
  
  <!-- Tagline -->
  <p class="logo-hero__tagline">
    <span>Precision</span>
    <span class="logo-hero__dot"></span>
    <span>Restoration</span>
    <span class="logo-hero__dot"></span>
    <span>Confidence</span>
  </p>
</div>

<style>
  .logo-hero {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
    padding: var(--spacing-sm) var(--spacing-md);
  }

  /* Container for logo layers */
  .logo-hero__container {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  /* Main logo - on TOP */
  .logo-hero__logo {
    display: block;
    width: auto;
    height: clamp(256px, 52vw, 440px);
    max-width: 95vw;
    position: relative;
    z-index: 2;
    filter: drop-shadow(0 2px 20px rgba(0, 0, 0, 0.15));
    opacity: 0;
    animation: logoFadeIn 0.8s ease-out 0.3s forwards;
    /* Ensure logo colors are preserved on mobile screenshots */
    color-scheme: light;
    forced-color-adjust: none;
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }

  @keyframes logoFadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Gold highlight - BEHIND the logo, reveals left to right */
  .logo-hero__highlight {
    position: absolute;
    top: 0;
    left: 0;
    width: auto;
    height: clamp(256px, 52vw, 440px);
    max-width: 95vw;
    z-index: 1;
    
    /* Start fully clipped (hidden) from the right side */
    clip-path: inset(0 100% 0 0);
    
    /* Animate the reveal from left to right, then STAY */
    animation: highlightReveal 1s ease-out 0.6s forwards;
  }

  @keyframes highlightReveal {
    0% {
      /* Fully hidden - clipped from right */
      clip-path: inset(0 100% 0 0);
    }
    100% {
      /* Fully revealed - no clipping */
      clip-path: inset(0 0 0 0);
    }
  }

  /* Respect reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .logo-hero__logo {
      animation: none;
      opacity: 1;
    }
    
    .logo-hero__highlight {
      animation: none;
      clip-path: inset(0 0 0 0);
    }
    
    .logo-hero__tagline {
      animation: none;
      opacity: 1;
    }
  }

  /* Tagline - right below INK */
  .logo-hero__tagline {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    font-family: var(--font-heading);
    font-size: clamp(0.9rem, 2.2vw, 1.15rem);
    font-weight: 400;
    font-style: italic;
    letter-spacing: 0.15em;
    color: var(--color-blush);
    opacity: 0;
    animation: taglineReveal 0.8s ease 1.5s forwards;
    margin: 0;
    margin-top: -80px;
    margin-bottom: 0;
    position: relative;
    z-index: 3;
  }

  @keyframes taglineReveal {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .logo-hero__dot {
    display: inline-block;
    width: 5px;
    height: 5px;
    background: var(--color-gold);
    border-radius: 50%;
    vertical-align: middle;
    opacity: 0.7;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .logo-hero__tagline {
      margin-top: -40px;
      font-size: clamp(0.75rem, 2vw, 0.9rem);
    }
  }

  @media (max-width: 600px) {
    .logo-hero__tagline {
      flex-wrap: wrap;
      justify-content: center;
      gap: var(--spacing-xs);
      margin-top: -30px;
    }
  }

  @media (max-width: 480px) {
    .logo-hero__tagline {
      display: none;
    }
  }
</style>
